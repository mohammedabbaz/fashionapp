/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        remotePatterns:[
            {hostname:'',
        pathname:"readymadeui.com",}
        ]
    }
};

export default nextConfig;
